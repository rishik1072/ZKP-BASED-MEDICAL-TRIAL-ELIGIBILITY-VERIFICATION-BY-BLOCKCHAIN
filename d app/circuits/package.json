{
  "name": "medical-zkp-circuits",
  "version": "1.0.0",
  "description": "Circom circuits for medical eligibility verification",
  "scripts": {
    "build": "npm run compile && npm run setup",
    "compile": "circom src/eligibility.circom --r1cs --wasm --sym -o dist",
    "setup": "node scripts/setup.js",
    "test": "node scripts/test.js"
  },
  "dependencies": {
    "snarkjs": "^0.7.0",
    "ffjavascript": "^0.2.57",
    "blake2b-wasm": "^2.4.0"
  },
  "devDependencies": {}
}
